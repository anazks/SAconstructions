<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Images</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <style>
        .box{

            margin-top: 100px;
            box-shadow: rgba(99, 99, 99, 0.2) 0px 2px 8px 0px;
        }
    </style>
    <!-- Navbar placeholder -->
    {{>Nav}}

    <!-- Container for the form -->
    <div class="container mt-5 " >
        <div class="row justify-content-center" style="margin-top: 10%;">
            <div class="col-md-8 col-lg-6 form-container p-5 box">
                <h2>Edit Files</h2>
                <form id="uploadForm" action="/admin/updateFile" method="post" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label for="description" class="form-label">Description</label>
                        <input name="description" type="text" class="form-control" id="description" placeholder="Type something about the picture" value="{{FilefromDb.description}}">
                    </div>
                    <div class="mb-3">
                        <label for="category" class="form-label">Category</label>
                        <select class="form-select" id="category" name="category" v>
                            <option value="previous-work">Previous Work</option>
                            <option value="gallery">Gallery</option>
                            <option value="Interior">Interior</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label for="file" class="form-label">Upload File</label>
                        <input type="file" name="image" class="form-control" id="file">
                    </div>
                    <input type="hidden" value="{{FilefromDb._id}}" name="id">
                    <button type="submit" class="btn btn-primary">Submit</button>
                </form>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS (Optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
